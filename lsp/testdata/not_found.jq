def fn: a;
def fn2: a(a; a);
def binop: a + a;
def unaryop: -a;
def pipe: a | a;
def comma: a, a;
def _as: a as $_ | a;
def paran: (a);
def array: [a, a];
def object: {a: a}, {(a): a}, {$a};
def _if: if a then a end;
def _if2: if a then a else a end;
def _if3: if a then a elif a then a else a end;
def _try: try a, try a catch a, a?;
def _reduce: reduce a as $_ (a; a);
def _foreach: foreach a as $_ (a; a);
def _foreach2: foreach a as $_ (a; a; a);
def string: "\(a)";
def _label: label $a | a | break $a;
